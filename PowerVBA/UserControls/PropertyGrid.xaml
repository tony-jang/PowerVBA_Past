<UserControl x:Class="PowerVBA.UserControls.PropertyGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             
             xmlns:local="clr-namespace:PowerVBA.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="propGrid" Grid.Row="1"/>
        </ScrollViewer>
        
        <Grid >
            <TextBlock Margin="10,0,0,0" x:Name="textBlock" HorizontalAlignment="Left" TextWrapping="Wrap" Text="ppt.Shapes" VerticalAlignment="Center"/>
        </Grid>
        


    </Grid>
</UserControl>
